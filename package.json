{
  "name": "nvmrc-checker",
  "version": "1.0.0",
  "description": "Check if .nvmrc exists in a directory or its subdirectories",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "nvmrc-checker": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "nvmrc",
    "nvm",
    "node version"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.16.1",
    "commander": "^11.1.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
